export type CoffeeBean = {
  id: string;
  name: string;
  roaster: string;
  origin: string;
  roastDate: string
  weight: number;
  gramsRemaining: number;
  isFinished: boolean;
  notes?: string;
  addedDate: string;
};
